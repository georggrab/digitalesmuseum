/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ToUrlPipe } from './to-url.pipe';

describe('Pipe: ToUrl', () => {
  it('create an instance', () => {
    let pipe = new ToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
